package bg.chat.common;

public enum MessageType {
    LOGIN,
    LOGIN_SUCCESS,
    LOGIN_FAIL,
    LIST_USERS,
    CREATE,
    CREATE_SUCCESS,
    CREATE_FAIL,
    JOIN,
    JOIN_SUCCESS,
    JOIN_FAIL,
    SEND_PRIVATE,
    SEND_GROUP,
    QUIT
}
